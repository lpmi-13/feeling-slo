export default function Results({ data }) {
    return (
        <div>
            <h3>
                Based on your swipes, these are the SLOs that you would be
                satisfied with
            </h3>
        </div>
    );
}
