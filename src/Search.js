export default function Search() {
    return (
        <div>
            <h2>
                This is the search page...click `&quot;`search`&quot;` and
                decide if the results load fast enough
            </h2>
        </div>
    );
}
